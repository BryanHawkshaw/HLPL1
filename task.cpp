#include "std_lib_facilities.h"
class Complex{
    public:
        complex number : complex();
        complex number (double re, double im);
        double modulus (Complex C); {
            re, im = C;
        }
        C() re{1}, im{2};
        double getNumber(double){
        return re, im;
    }

    private:
        double re;
        double im;
};

    void() {
        complex () :: double modulus(Complex c)
    }

Complex operator+ {
    cout << re + im << endl;
}

Complex operator<< {
    cout << re 
}
int main() {
    modulus :: scope resolution operator;
    return 0;
}